// Tipo base para os dados do slider
export const createSliderItem = (id, title, description, image, instructor, duration, level, room) => ({
    id,
    title,
    description,
    image,
    instructor,
    duration,
    level,
    room
});